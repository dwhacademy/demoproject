BEGIN TRANSACTION;
DROP TABLE IF EXISTS dev_demo_al.dim_store CASCADE;

CREATE TABLE dev_demo_al.dim_store
(
    store_id BIGINT NOT NULL,
    state VARCHAR(255),
    store_nm VARCHAR(255),
    phone VARCHAR(255),
    email VARCHAR(255),
    city VARCHAR(255),
    PRIMARY KEY(store_id)
);
CALL dev_demo_ml.sp_deployment_objects('dim_store', 'dev_demo_al');
END TRANSACTION;
